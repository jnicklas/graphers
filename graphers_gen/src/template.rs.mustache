extern crate graphers_core;
extern crate serde;

use std::borrow::Cow;
use serde::{Serialize, Serializer};
use serde::ser::{MapVisitor};
use self::graphers_core::*;

// EXTRACTABLE

struct Selection<'a, T> where T: 'a + Select {
    target: T,
    selection_set: &'a [query::Selection],
}

impl<'a, T> Serialize for Selection<'a, T> where T: 'a + Select {
    fn serialize<S>(&self, serializer: &mut S) -> Result<(), S::Error> where S: Serializer {
        serializer.serialize_map(SelectionStructVisitor {
            target: &self.target,
            iter: self.selection_set.iter(),
        })
    }
}

struct SelectionStructVisitor<'a, T, I> where T: 'a + Select, I: Iterator<Item=&'a query::Selection> {
    target: &'a T,
    iter: I,
}

impl<'a, T, I> MapVisitor for SelectionStructVisitor<'a, T, I> where T: 'a + Select, I: Iterator<Item=&'a query::Selection> {
    fn visit<S>(&mut self, serializer: &mut S) -> Result<Option<()>, S::Error> where S: Serializer {
        match self.iter.next() {
            Some(selection) => {
                self.target.select(selection, serializer)
            },
            None => Ok(None)
        }
    }
}

pub trait Select {
    fn select<S>(&self, selection: &query::Selection, serializer: &mut S) -> Result<Option<()>, S::Error> where S: Serializer;
}

// END EXTRACTABLE

{{#query_root}}
pub struct QueryResult<'a, T>(Selection<'a, {{name}}<T>>) where T: 'static + Resolve{{name}};
{{/query_root}}

////////////////////////////////////////////////////////////////////////////////
// RESOLVE TRAITS
// Implement these in your own code to resolve the fields in a query
////////////////////////////////////////////////////////////////////////////////

pub trait Schema: 'static {
    {{#objects}}
    type {{name}}: Resolve{{name}};
    {{/objects}}

    {{#query_root}}
    fn query<'a>(&self, query: &'a query::Query) -> QueryResult<'a, Self::{{name}}> {
        QueryResult(Selection { target: {{name}}(self.root()), selection_set: query.selection_set() })
    }

    fn root(&self) -> Self::{{name}};
    {{/query_root}}
}

pub trait HasSchema {
    type Schema: Schema;
}

{{#objects}}
pub trait Resolve{{name}}: HasSchema + Sized {
    {{#fields}}
    fn {{name}}(&self, {{{parameters}}}) -> {{{ty}}};
    {{/fields}}
}

{{/objects}}

////////////////////////////////////////////////////////////////////////////////
// PLUMBING
// Everything after this line is plumbing machinery, you can ignore it
////////////////////////////////////////////////////////////////////////////////

{{#query_root}}
impl<'a, T> Serialize for QueryResult<'a, T> where T: 'static + Resolve{{name}} {
    fn serialize<S>(&self, serializer: &mut S) -> Result<(), S::Error> where S: Serializer {
        self.0.serialize(serializer)
    }
}
{{/query_root}}

{{#objects}}

struct {{name}}<T>(T) where T: 'static + Resolve{{name}};

struct {{name}}Ref<'a, T>(&'a T) where T: 'a + Resolve{{name}};

impl<T> Select for {{name}}<T> where T: Resolve{{name}} {
    fn select<S>(&self, selection: &query::Selection, serializer: &mut S) -> Result<Option<()>, S::Error> where S: Serializer {
        {{name}}Ref(&self.0).select(selection, serializer)
    }
}

impl<'a, T> Select for {{name}}Ref<'a, T> where T: 'a + Resolve{{name}} {
    fn select<S>(&self, selection: &query::Selection, serializer: &mut S) -> Result<Option<()>, S::Error> where S: Serializer {
        match selection {
            &query::Selection::Field(ref field) => {
                match field.name().as_str() {
                    {{#fields}}
                    "{{name}}" => {
                        let target = Resolve{{object_name}}::{{name}}(self.0, {{{arguments}}});
                        {{{preserialize}}}
                        Ok(Some(try!(serializer.serialize_map_elt(&field.alias().as_str(), target))))
                    },
                    {{/fields}}
                    name => panic!("unknown field {}", name),
                }
            },
            &query::Selection::FragmentSpread(_) => {
                panic!("not implemented!");
            },
            &query::Selection::InlineFragment(_) => {
                panic!("not implemented!");
            },
        }
    }
}

{{/objects}}

{{#interfaces}}
pub trait Has{{name}} {
    type {{name}};
}

pub enum {{name}}<S> where S: Schema {
    {{#implementors}}
    {{name}}(S::{{name}}),
    {{/implementors}}
}

impl<S> {{name}}<S> where S: Schema {
    {{#fields}}
    fn {{field_name}}(&self, {{{parameters}}}) -> {{{ty}}} {
        match self {
            {{#implementors}}
                &{{interface_name}}::{{name}}(ref target) => target.{{field_name}}({{{parameter_names}}}),
            {{/implementors}}
        }
    }
    {{/fields}}
}

impl<T> Has{{name}} for T where T: HasSchema {
    type {{name}} = {{name}}<T::Schema>;
}

impl<'a, T> Select for {{name}}<T> where T: Schema {
    fn select<S>(&self, selection: &query::Selection, serializer: &mut S) -> Result<Option<()>, S::Error> where S: Serializer {
        match selection {
            &query::Selection::Field(ref field) => {
                match field.name().as_str() {
                    {{#fields}}
                    "{{name}}" => {
                        let target = self.{{name}}({{{arguments}}});
                        {{{preserialize}}}
                        Ok(Some(try!(serializer.serialize_map_elt(&field.alias().as_str(), target))))
                    },
                    {{/fields}}
                    name => panic!("unknown field {}", name),
                }
            },
            &query::Selection::FragmentSpread(_) => {
                panic!("not implemented!");
            },
            &query::Selection::InlineFragment(ref fragment) => {
                match fragment.on().as_str() {
                    {{#implementors}}
                    "{{name}}" => {
                        if let &{{interface_name}}::{{name}}(ref target) = self {
                            let iter = fragment.selection_set().iter();
                            let mut visitor = SelectionStructVisitor { target: &{{name}}Ref(target), iter: iter };
                            while let Some(_) = try!(MapVisitor::visit(&mut visitor, serializer)) {};
                        }
                    }
                    {{/implementors}}
                    other => panic!("unknown fragment type: {}", other),
                }
                Ok(Some(()))
            },
        }
    }
}
{{/interfaces}}
